name: Deploy to Staging

on:
  push:
    branches:
      - main

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show target environment
        run: echo "Entorno objetivo: staging"

      - name: Simulate staging deploy
        run: echo "Desplegando aplicación a staging..."
